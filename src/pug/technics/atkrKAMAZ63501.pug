

mixin tech(name)
  div(id="" + name class="tech")

mixin ident(identifikator)
  div(id="" + identifikator class="card-body")

- var subPage= 'Автокран КАМАЗ-63501 40 тонн', value = "Спецтехника", name = 'atkrKAMAZ63501', identifikator = 'autokran', modalHidden = true;
- 

doctype html
html(lang="ru")
    include ../head
    body
      include ../header
      include ../strip
      include ../technic
      .row.descr
        .col
          p 
      include ../footer
      script(src="../build/js/all.js")
