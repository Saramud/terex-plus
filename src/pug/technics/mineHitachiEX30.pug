

mixin tech(name)
  div(id="" + name class="tech")

mixin ident(identifikator)
  div(id="" + identifikator class="card-body")

- var subPage= 'Мини-экскаватор HITACHI EX30 UR2', value = "Спецтехника", name = 'mineHitachiEX30', identifikator = 'mine', modalHidden = true;
- 

doctype html
html(lang="ru")
    include ../head
    body
      include ../header
      include ../strip
      include ../technic
      include ../footer
      script(src="../build/js/all.js")
